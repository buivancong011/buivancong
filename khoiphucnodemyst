#!/bin/bash

# NHẬN DIỆN IP
OLD_IP_1="$1"
OLD_IP_2="$2"

if [ -z "$OLD_IP_1" ] || [ -z "$OLD_IP_2" ]; then
    echo "❌ LỖI: Thiếu IP đầu vào."
    exit 1
fi

# ================= CẤU HÌNH =================
# (Giữ nguyên Token cũ của ông)
MY_TOKEN='{"access_token":"ya29.a0AUMWg_Kv0s3-JmIBl3jGwG1Pwn-0877B8JiOenW0J1O2C0gV52r3tQZCMC_a6Y-r_nRUh-AIlbb61qyCiosp4j4BUoPZZykHd3YTJDQ7x3o1kL5n84E-SfZV_nNFDlh_gElAFX_TwO-R97Hg-BIT4wAIwv41m6eWsP9_5eeG4glGLKttsbb_hG4EjwgRzp3HxDyPItEaCgYKAe4SARISFQHGX2MiFWO2VNZwCbdqMice83bxeQ0206","token_type":"Bearer","refresh_token":"1//0gYKP3t9gNLbcCgYIARAAGBASNwF-L9Ir1JCy9-XxItZRooJntSaxU1pkFCpfJFbdbsVtvGy7SCMdhavxXRN_Ky-tXfBUqF_dfXA","expiry":"2026-02-12T13:42:42.0402294+07:00"}'
RCLONE_REMOTE="gdrive:Mysterium_Backups"
TEMP_DIR="/root/restore_temp"

# CÀI ĐẶT NHANH
if ! command -v rclone &> /dev/null; then
    curl https://rclone.org/install.sh | sudo bash >/dev/null 2>&1
fi
if ! command -v docker &> /dev/null; then
    curl -fsSL https://get.docker.com | sh >/dev/null 2>&1
    systemctl enable --now docker
fi

# TẠO CONFIG RCLONE (Ghi đè để chắc chắn đúng)
mkdir -p /root/.config/rclone
cat << EOF > /root/.config/rclone/rclone.conf
[gdrive]
type = drive
scope = drive
token = $MY_TOKEN
EOF
mkdir -p "$TEMP_DIR"

# KIỂM TRA KẾT NỐI DRIVE NGAY LẬP TỨC
if ! rclone listremotes | grep -q "gdrive"; then
    echo "❌ LỖI: Không kết nối được Google Drive (Token sai hoặc Config lỗi)."
    exit 1
fi

# HÀM KHÔI PHỤC (SỬA LẠI: LỖI LÀ EXIT LUÔN)
restore_process() {
    local ID=$1
    local TARGET_IP=$2
    local NODE_SUFFIX="myst$ID"
    local VOL_NAME="myst-data$ID"

    echo "🔍 Đang tìm backup cho Node $ID (Nguồn: $TARGET_IP)..."

    # Tìm file
    FILE_NAME=$(rclone lsl "$RCLONE_REMOTE" | grep "$TARGET_IP" | grep "$NODE_SUFFIX" | sort -k2,2 -k3,3 | tail -n 1 | awk '{print $4}')

    if [ -z "$FILE_NAME" ]; then
        echo "❌ LỖI: Không tìm thấy file backup nào chứa '$TARGET_IP' và '$NODE_SUFFIX' trên Drive!"
        exit 1  # <--- QUAN TRỌNG: Thoát ngay để báo lỗi
    fi

    echo "📥 Tải về: $FILE_NAME"
    if ! rclone copyto "$RCLONE_REMOTE/$FILE_NAME" "$TEMP_DIR/backup_$ID.tar"; then
        echo "❌ LỖI: Tải file thất bại."
        exit 1
    fi

    echo "♻️  Bung nén vào $VOL_NAME..."
    # Tạo volume và đảm bảo nó tồn tại
    docker volume create "$VOL_NAME" >/dev/null 2>&1
    
    # Xả nén (Nếu lệnh tar lỗi thì báo lỗi luôn)
    if ! docker run --rm -v "$VOL_NAME":/data -v "$TEMP_DIR":/backup alpine tar xf "/backup/backup_$ID.tar" -C /data; then
         echo "❌ LỖI: Bung nén vào Docker Volume thất bại."
         exit 1
    fi

    rm -f "$TEMP_DIR/backup_$ID.tar"
    echo "✅ Xong Node $ID"
}

# CHẠY
restore_process 1 "$OLD_IP_1"
restore_process 2 "$OLD_IP_2"

rm -rf "$TEMP_DIR"
# CHỈ IN DÒNG NÀY NẾU MỌI THỨ OK
echo "RESTORE COMPLETE"
